{
  "name": "saucedemo-automation",
  "version": "1.0.0",
  "description": "Playwright automation for SauceDemo checkout flow",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
